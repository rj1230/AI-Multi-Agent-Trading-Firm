# AI Multi-Agent Trading Firm
**Paper trading only — no real capital at risk.**

A multi-agent trading system built around one core idea: LLM reasoning is
used only where judgment is genuinely needed (interpreting a headline,
reading a chart pattern). Everything else — merging signals, enforcing risk
limits, deciding hold-vs-trade — is deterministic, auditable code.

